<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pethoalpar.myapplication">

    <uses-feature
        android:name="android.hardware.camera2"
        android:required="true" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="WEEDKSS"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".activity_menu_principal" />
        <activity
            android:name=".ContadorPersonasActivity"
            android:label="@string/title_activity_contador_personas"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".DeteccionObjetosActivity"
            android:label="@string/title_activity_deteccion_objetos"
            android:theme="@style/AppTheme" />
        <activity android:name=".detectar_objeto" />
    </application>

</manifest>